# Sequence Gate Kit Builder Prompt Run 004

Build the next cumulative Kit Builder app focused on the NexusRealtime gated new-game workflow.

The app should show a sequence and gate workflow surface where each phase has a goal, input artifact, output artifact, gate, pass criteria, loopback target, and recheck point.

Carry forward the best known capabilities from earlier runs and keep the public gallery capped at 10 active builds.

Expose state through window.GameHost.getState(), keep UI concise, and write LEDGER-style run artifacts.
